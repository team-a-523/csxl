/**
 * Placeholder suite for legacy application-form.spec.ts path.
 *
 * Real component behavior is covered in application-form.component.spec.ts.
 */

describe('application-form.spec.ts placeholder', () => {
  it('keeps this legacy suite path valid', () => {
    expect(true).toBe(true);
  });
});
